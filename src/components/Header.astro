---

---

<header>
  <img src="/gatofreela.png" alt="Logotipo da Gatofreela" />
  <h1>GATO FREELA</h1>
</header>

<style>
@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap');

header {
  background: var(--brown-400);
  display: flex;
  justify-content: center;
  align-items: center;
  border-bottom-left-radius: 10%;
  border-bottom-right-radius: 10%;
  padding: 1.25rem 0;
  height: 6rem;
  margin-bottom: 1.5rem;
}

header img {
  height: 6rem;
  margin-right: 1rem;
  opacity: 0;
  animation: fadeIn 1s 0.5s forwards, slideIn 1s ease-out 0.5s forwards;
}

header h1 {
  font-weight: 800;
  font-size: 2.5rem;
  font-family: 'Bebas Neue', sans-serif;
  text-shadow: 0px 1.2px 0px #555;
  color: var(--gray-100);
  margin-top: 1rem;
  opacity: 0;
  animation: fadeIn 1s 1s forwards, slideUp 1s ease-out 1s forwards;
}


@keyframes fadeIn {
  0% {
    opacity: 0;
  }
  100% {
    opacity: 1;
  }
}

@keyframes moveUp {
  0% {
    transform: translateY(20px);
  }
  100% {
    transform: translateY(0);
  }
}

@keyframes slideIn {
  0% {
    transform: translateY(20px);
  }
  100% {
    transform: translateY(0);
  }
}

@keyframes slideUp {
  0% {
    transform: translateY(20px);
  }
  100% {
    transform: translateY(0);
  }
}
</style>